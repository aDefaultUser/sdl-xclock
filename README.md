# sdl-xclock – Analog Clock (SDL3)

A borderless, always‑on‑top analog clock.

## Build

```bash
git clone git@github.com:aDefaultUser/sdl-xclock.git
cd sdl-xclock
mkdir build && cd build
cmake ..
cmake --build .